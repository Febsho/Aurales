import { beforeEach,describe,expect,it,vi } from 'vitest'
import { loadRecommendationFeedback,saveRecommendationFeedback } from './feedbackStore'

describe('recommendation feedback persistence',()=>{beforeEach(()=>{const values=new Map<string,string>();vi.stubGlobal('localStorage',{getItem:(key:string)=>values.get(key)||null,setItem:(key:string,value:string)=>values.set(key,value)});vi.stubGlobal('window',{dispatchEvent:vi.fn()});});it('persists hidden titles across reloads',()=>{saveRecommendationFeedback({id:'x',title:'Hidden',type:'movie',provider:'tmdb',tmdbId:7},'hide');expect(loadRecommendationFeedback()).toMatchObject([{kind:'hide',mediaKey:'movie:tmdb:7'}])});it('replaces earlier feedback for the same title',()=>{const item={id:'x',title:'Title',type:'movie' as const,provider:'tmdb',tmdbId:7};saveRecommendationFeedback(item,'less-like-this');saveRecommendationFeedback(item,'more-like-this');expect(loadRecommendationFeedback()).toHaveLength(1);expect(loadRecommendationFeedback()[0].kind).toBe('more-like-this')})})
